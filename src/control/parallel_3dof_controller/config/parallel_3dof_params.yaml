# 3-DOF并联控制器配置文件

/**:
  ros__parameters:
    # 几何参数 (米)
    l0: 0.02  # 平台半径
    l1: 0.01  # 动平台到O点的距离
    l2: 0.03  # 静平台到O点的距离

    # 控制参数
    ankle_side: 'right'  # 控制哪侧脚踝 ('right' 或 'left')
    default_speed: 100   # 默认舵机速度 (毫秒)

    # 调试选项
    debug: false  # 是否打印调试信息
